doctype html
html
    head
        title Home
        link(rel='stylesheet', href='/css/styles.css')
    body
        header 
            h1 Buscador de películas
        form(action='/film', method='get')
            input(type='text', name='title', placeholder='Título de la película')
            button(type='submit') Buscar película
    include footer.pug 